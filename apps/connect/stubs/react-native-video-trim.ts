/**
 * Metro resolves `react-native-video-trim` to this stub on web so the bundle never loads
 * the native TurboModule (see metro.config.js).
 */
export function showEditor(_filePath: string, _config?: Record<string, unknown>): void {
  if (__DEV__) {
    console.warn('[react-native-video-trim stub] showEditor is a no-op on web');
  }
}
